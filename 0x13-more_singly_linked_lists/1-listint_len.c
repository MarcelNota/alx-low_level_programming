#include "lists.h"

/**
 * listint_len - elements
 * @h: the list
 * Return: the nodes
 */
size_t listint_len(const listint_t *h)
{
	 size_t elem = 0;
while (h)
elem++;
h = h->next;
}
return (elem);
}
